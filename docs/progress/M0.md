# M0 — diario

## 2026-09-24
- **Fatto:** workspace Rust (9 crate stub + `vetro-diff`), nightly fissato,
  CLAUDE.md, PLAN.md, ADR 0001–0003, CI (push + notturna), `tools/ci.sh`,
  subagenti CPU e Qualità, skill del test differenziale. Oracolo: ELF minimi
  generati in Rust; `qemu_exit_code` (uscita 42) e `qemu_stdout` ("vetro\n")
  verdi sotto QEMU reale (wrapper Docker su macOS) con
  `VETRO_REQUIRE_ORACLE=1`. Verificato anche che senza oracolo e con la
  variabile i test falliscono. fmt, clippy `-D warnings` e build wasm32 verdi.
- **Manca:** primo push su un remoto per vedere la CI GitHub verde (criterio
  di uscita "in CI"). RISU non ancora integrato (serve da M1: build nel
  container oracolo).
- **Bloccato:** niente.
- Aggiunti LICENSE.md (PolyForm Noncommercial 1.0.0, ADR 0004) e remoto `origin` (github.com/1vcian/Vetro).

## 2026-09-24 (chiusura)
- **Fatto:** CI GitHub verde al primo push (job native e wasm, run 36021646157): criterio di uscita M0 soddisfatto. Licenza cambiata in PolyForm Noncommercial 1.0.0 (ADR 0004), nome "Vetro" ufficiale, README in inglese.
- **Manca:** RISU, spostato in M1/M2 (vedi ADR successivi).
- **Bloccato:** niente.
