# M5 — diario

## 2026-09-25 (preparazione dispositivi, agente Piattaforma)
- **Fatto:**
  - In `vetro-platform`: virtio-gpu 2D (`virtio/gpu.rs`, comandi, errori e
    cursore come QEMU 10.0, trait `DisplayBackend` con `MemDisplay`),
    generatore EDID identico a quello di QEMU (`virtio/edid.rs`),
    virtio-input con i profili tastiera, tablet e touchscreen di QEMU
    (`virtio/input.rs`), virtio-vsock con l'host dentro il dispositivo
    (`virtio/vsock.rs`: ascolto, connessioni nei due versi, credito,
    chiusure, reset del trasporto, ordine deterministico). 30 test unitari
    nuovi (133 in tutto nel crate), più uno in `vetro-machine`.
  - In `vetro-machine`: `Devices` e `Machine::with_devices` (default GPU
    1280x800, tastiera, tablet; vsock a richiesta) negli slot 31..28 come i
    `-device` di QEMU; accesso dell'host con `Machine::gpu/keyboard/pointer/vsock`.
  - Kernel guest: DRM + virtio-gpu (KMS, senza fbdev), evdev +
    virtio-input, vsock + virtio-vsock; `vetro-dev` (C statico, header UAPI
    del kernel) nell'initramfs; l'autotest esegue modeset, DIRTYFB e
    cursore, stampa EDID, `/proc/bus/input/devices` e le capacità evdev.
  - Oracolo: `QEMU_MACHINE` con `-global virtio-mmio.force-legacy=false`
    (QEMU di default presenta virtio-mmio legacy e i driver di GPU e input
    falliscono) e gli stessi tre `-device`. Log di Vetro uguale a QEMU 10.0
    (Docker) con le sole differenze note di prima; l'output dei dispositivi
    è identico anche con QEMU 8.2 (quello della CI). Riferimento rigenerato.
  - `tests/boot/tests/devices.rs` (solo Vetro): pixel dello scanout e
    cursore contro il motivo del guest, tasti e tablet iniettati e letti
    con evdev, LED dal guest, vsock nei due versi (300 KB oltre il credito
    del guest, 200 KB di eco); due esecuzioni identiche (161536482
    istruzioni).
  - L'avvio del test `vetro` passa da circa 98 a 138,6 milioni di
    istruzioni: il guest disegna 1280x800 pixel e prova il modeset.
- **Manca:** backend WebGPU/canvas nel browser (agente Web) e
  collegamento di input e vsock alla pagina; adb sopra vsock (serve
  l'immagine Android); confronto di virtio-vsock con QEMU (vedi sotto);
  risoluzione verticale per Android da scegliere col profilo del
  dispositivo (`GpuConfig`, `set_display`).
- **Bloccato:** vsock contro QEMU: `vhost-vsock-device` vuole
  `/dev/vhost-vsock`, che Docker Desktop (kernel linuxkit) e i runner non
  hanno; resta verificato solo col driver Linux sotto Vetro.
  `MachineConfig` non ha un campo `devices` perché `vetro-wasm` lo
  costruisce elencando i campi (area dell'agente Web): se passa a
  `..MachineConfig::default()` si può unire.

## 2026-09-25 (avvio del GKI android15-6.6 sotto QEMU e sotto Vetro)
- **Fatto:** l'immagine dell'emulatore SDK Android 15 arm64 (solo in
  locale, `tools/android-emu/`) arriva sotto Vetro dove arriva sotto QEMU
  con la stessa macchina: prima e seconda fase di init, super e `/data`
  cifrato montati, `bpfloader`, zygote (135,8 s di guest contro 60,6), poi
  surfaceflinger abortisce per la grafica goldfish/gfxstream e zygote
  riparte a ciclo, in tutti e due. `vetro boot` ha `--no-devices`,
  `--disk=FILE` (copy-on-write sopra il file), `--guest-secs`, `--stats`
  (test `crates/vetro-cli/tests/boot_disk.rs`). Divergenza corretta: GPIO
  PL061 + `gpio-keys` (tasto di accensione) come QEMU virt, con test.
  Dettagli in `docs/research/m5-avvio-gki.md`.
- **Manca:** grafica (immagine nostra con SwiftShader/drm_hwcomposer sulla
  virtio-gpu 2D), virtio-rng deterministico (`prng_seeder`), PMU; la
  pressione del tasto dall'host va collegata alla pagina e al replay.
- **Bloccato:** niente; `jitterentropy` fallisce per il tempo deterministico
  (differenza voluta, non fatale).

## 2026-09-25 (rete del guest con vetro-net, agente Rete)
- **Fatto:**
  - `vetro-machine`: virtio-net collegato allo stack di `vetro-net` con il
    sinkhole (`net.rs`, `NetLink`, `NetSetup`; `Devices::net`, di default
    presente, slot 28 dopo GPU, tastiera e tablet; MAC 52:54:00:12:34:56
    come QEMU). Tempo dello stack = CNTPCT in microsecondi; `poll` alla
    scadenza dello stack, che entra nella prossima scadenza della macchina
    (limita il JIT, sveglia la WFI). Niente `VIRTIO_NET_F_CSUM`.
    `Machine::net` (mutabile) e `Machine::net_view` (sola lettura).
  - `vetro-net`: `Display` per `NetEvent`, `Flow`, `Mac`; `PartialEq/Eq`
    per `NetConfig` e `SinkholeConfig`.
  - Kernel guest: `CONFIG_PACKET` (udhcpc); initramfs con
    `/usr/share/udhcpc/default.script`; l'autotest fa DHCP, stampa rotte e
    `resolv.conf`, ping a gateway e DNS. `QEMU_MACHINE` con `-netdev
    user,id=n -device virtio-net-device,netdev=n`: log di Vetro uguale a
    QEMU 10.0 (Docker), riferimento rigenerato (i numeri `virtioN` degli
    input scalano di uno: virtio-net è il dispositivo con l'indirizzo più
    basso). Stesse istruzioni col JIT nativo e in Node (256776671; l'avvio
    passa da 138,6 a 256,8 milioni di passi perché `ping -c 2` aspetta un
    secondo di guest, saltato nella WFI: 3,1 s reali).
  - `tests/boot/tests/net.rs` (solo Vetro): DHCP, `nslookup` →
    198.18.0.1, GET breve, GET da 300 KB (`cksum` uguale all'host), POST da
    108 KB ritrovata byte per byte nel sinkhole, ping al gateway e a un
    indirizzo finto; l'host verifica registro (DHCP, DNS, connessioni con
    nome, byte, chiusure `Normal`) e sinkhole; due esecuzioni identiche
    (796890759 istruzioni, 148 eventi).
  - `vetro boot`: rete di default, `--no-net`, `--net` (anche dopo
    `--no-devices`), `--net-events` (registro su stderr); test
    `crates/vetro-cli/tests/boot_net.rs`.
  - kselftest ricostruiti col nuovo kernel e initramfs: 95 test, 80 ok o
    SKIP sotto QEMU, esiti uguali sotto Vetro (62136905469 istruzioni).
- **Manca:** relay WebSocket come upstream dietro `NetLink` e scelta
  dell'upstream da `NetSetup` (M7); stato dello stack negli snapshot (M6);
  registrazione degli accessi dell'host (`Machine::net`) per il replay
  (M10); collegamento del registro alla pagina (agente Web).
- **Bloccato:** niente. Con `-netdev user` il confronto con QEMU copre solo
  DHCP e ping al gateway/DNS: DNS e TCP di QEMU escono sulla rete vera.

## 2026-09-25 (app web, display, input e disco via HTTP Range, agente Web)
- **Fatto:** `web/app` (moduli ES, niente bundler né npm): macchina in un
  Worker, scanout di virtio-gpu su canvas 2D (WebGPU a scelta) coi soli
  rettangoli cambiati trasferiti, cursore, console seriale con terminale,
  tastiera (`KeyboardEvent.code` → evdev), mouse/tocco assoluti, rotella,
  tasto di accensione (GPIO 3), rete col sinkhole a scelta;
  `tools/web-serve.mjs` (Range, COOP/COEP: la pagina è
  `crossOriginIsolated`). vetro-wasm ABI 3 (`docs/specs/wasm.md`):
  `vetro_machine_new_with`, display RGBA (`WebDisplay`), cursore,
  virtio-input, GPIO, dischi con i dati dal JS (`HostDisk` sotto
  copy-on-write) e arresto `Blocked`. `vetro-machine`: `Stop::Blocked`,
  tempo del guest fermo mentre un disco aspetta dati (ADR 0014, test
  `disco_non_pronto_ferma_il_tempo_del_guest`). `web/node/disk.mjs`:
  `RangeSource`, `BlobSource`, cache OPFS, `DiskFeeder`. Test
  (`tools/web-test.sh`, passo nel job `boot`): disco via HTTP con cache
  vuota, piena, blocchi da 4 KiB e lettura anticipata = disco locale =
  riferimento nativo (182896020 istruzioni, log byte per byte, anche con
  l'interprete); framebuffer letto via API uguale al motivo del guest
  pixel per pixel, cursore, tastiera, tablet, LED = nativo (147890642
  istruzioni); l'app in Chrome headless via DevTools (canvas pixel per
  pixel, tasto vero al guest, riavvio con i blocchi da OPFS).
- **Manca:** immagine Android da servire; registro degli ingressi della
  pagina salvato e rigiocabile (M10, oggi solo in memoria nel Worker);
  copy-on-write persistente (M6); vsock e registro di rete collegati alla
  pagina; prova di WebGPU nel browser headless (lì `--disable-gpu`); thread
  WASM (le intestazioni COOP/COEP ci sono già).
- **Bloccato:** niente. Il test nel browser dice SKIP se il runner non ha
  Chrome (`VETRO_CHROME`, `VETRO_REQUIRE_BROWSER=1` per renderlo
  obbligatorio): da verificare sul runner ARM della CI.

## 2026-09-25 (inoltro di porte TCP verso il guest, agente Rete)
- **Fatto:** `vetro-net` fa anche da client TCP verso il guest
  (`Stack::host_connect`/`host_send`/`host_recv`/`host_shutdown`/
  `host_abort`/`host_conn`/`host_release`, modulo `hostfwd`): SYN da
  10.0.2.2 con porte effimere 49152.. come slirp, stato `SynSent` nella
  stessa macchina a stati, code da 256 KiB con contropressione nei due
  versi, evento `TcpConnect`; 9 test col finto guest. `vetro boot
  --hostfwd=tcp:[ADDR]:PORTA-:PORTA_GUEST` (socket veri in `vetro-cli`,
  byte nella macchina tra un quanto e l'altro). vetro-wasm ABI 5
  (`vetro_net_*`) e `GuestSocket` in `web/node/vetro.mjs`. Col kernel M3
  (`tests/boot/tests/hostfwd.rs`, `nc -n -v -l -p 5555 -e cat`): eco di
  200 KB, chiusure dai due lati, `Refused`, reset dall'host e dal guest,
  due esecuzioni uguali; confronto con QEMU `hostfwd`: stesse righe di
  `nc` (`connect to 10.0.2.15:5555 from 10.0.2.2:…`) e stessa eco. Test
  con un `TcpStream` vero (`crates/vetro-cli/tests/boot_hostfwd.rs`) e in
  Node (`tests/web/hostfwd.mjs`, JIT e interprete, in `tools/web-test.sh`).
  Il wrapper Docker di QEMU accetta `VETRO_ORACLE_NAME` (client dentro il
  container: le connessioni inoltrate da Docker arrivano da 172.17.0.1).
  Sopra gli snapshot (ADR 0015): connessioni dell'host, code, stato
  `SynSent` e porta effimera salvati (`FORMAT_VERSION` 2), taglio a metà
  eco in `tests/boot/tests/snapshot.rs` e in `vetro-net`; vetro-wasm ABI 5.
- **Manca:** registro degli ingressi di rete dell'host con il numero di
  istruzione (M10: oggi `--hostfwd` con connessioni non è ripetibile);
  client ADB in JS sopra `GuestSocket` (M5/M6); UDP inoltrato; UI nella
  pagina.
- **Bloccato:** niente. adb vero da provare quando c'è l'immagine Android
  con adbd su TCP 5555 (`docs/specs/net.md`, "Come adb userà l'inoltro").

## 2026-09-25 (caricatore di immagini Android, agente Piattaforma)
- **Fatto:** `vetro_machine::android` (ADR 0018, `docs/specs/android-boot.md`):
  `boot.img` v0–v4, `vendor_boot.img` v3/v4 (tabella dei ramdisk, board id,
  bootconfig), `init_boot.img`; kernel gzip/LZ4 legacy/LZ4 frame con
  decompressori propri senza dipendenze; ramdisk del vendor in ordine di
  tabella (recovery solo con `--recovery`) + generico senza allineamento;
  riga di comando boot + vendor + bootloader; con vendor v4 gli
  `androidboot.*` del bootloader nel blocco bootconfig in coda all'initrd
  (byte uguali a `tools/bootconfig -a` del kernel) e `bootconfig` aggiunto
  se manca. `Machine::load_android`; `vetro boot --boot-img/--vendor-boot/
  --init-boot/--recovery/--android-dump` (opzioni anche nella forma
  `--opzione valore`). `mkbootimg.py` di AOSP fissato in `tools/mkbootimg/`.
  Kernel guest con `CONFIG_BOOT_CONFIG=y` e `CONFIG_RD_LZ4=y`, riferimento
  di QEMU rigenerato. Test: unitari (vettori di gzip/lz4 veri, errori),
  `tests/mkbootimg.rs` (v0–v4 da mkbootimg, sezione per sezione),
  `tests/boot/tests/android.rs` (boot v4 gzip + init_boot LZ4 + tre
  frammenti del vendor: `/proc/cmdline`, `/proc/bootconfig` e file uguali
  sotto Vetro e sotto QEMU con lo stesso spacchettamento, log uguali salvo
  `KNOWN_DIFFERENCES`), `vetro-cli/tests/boot_android.rs`. Prova locale
  (non in CI) con l'immagine SDK android-36: `kernel-ranchu` (gzip con
  FNAME) in un boot.img v4 → `Image` di 36 MB uguale a `gunzip`, in 0,2 s;
  `ramdisk.img` (LZ4 legacy) passato intatto.
- **Manca:** scelta dei frammenti per `board_id`, AVB/vbmeta e firma GKI
  (ignorati); chiavi dei parametri che ripetono la sezione bootconfig del
  vendor non controllate (il kernel scarta il blocco); nessuna immagine
  Cuttlefish/GKI vera provata (l'SDK non ha boot.img); `--android-dump` non
  ancora usato da `tools/android-emu`.
- **Bloccato:** niente.

## 2026-09-25 — sito su GitHub Pages

- **Fatto:** `tools/pages/build.sh` costruisce il sito (pagina d'ingresso in
  inglese, `web/app` e `web/node`, vetro-wasm, kernel e initramfs di M3, e in
  `sources/` i sorgenti GPL esatti: tarball del kernel a pezzi da 60 MiB con
  sha256, BusyBox 1.37.0 e patch di Alpine aports al commit di -r20,
  configurazione e script); gli URL predefiniti dell'app sono relativi, così
  funziona sotto `/Vetro/`. `tests/web/pages.mjs` lo serve sotto `/Vetro/`
  senza COOP/COEP (come Pages) e in Chrome va dalla pagina d'ingresso alla
  shell (2,2 s) e controlla i sorgenti ricomposti. Job `pages` e `deploy` in
  CI: si pubblica da main solo con tutti gli altri job verdi. Aiuti di Chrome
  spostati in `tests/web/chrome.mjs`.
- **Manca:** i thread WASM (più CPU) richiederanno COOP/COEP, che Pages non
  manda: servirà un service worker che li aggiunga. Nessuna immagine Android
  sul sito finché non esistono le nostre (artefatti versionati).
- **Bloccato:** la prima pubblicazione richiede che Pages sia attivo con
  sorgente "GitHub Actions" (Settings → Pages) se `configure-pages` non può
  attivarlo col token del workflow.

## 2026-09-26 — immagine AOSP 15 di Vetro (agente Guest, ADR 0022)
- **Fatto:** prodotto `vetro_arm64-bp1a-userdebug` (android-15.0.0_r36) sopra
  `vsoc_arm64_only` di Cuttlefish: virtio_mmio nella prima fase, bootconfig
  nel vendor_boot (32 slot in `boot_devices`, grafica guest_swiftshader),
  fstab senza AVB, microG v0.3.16.252432 privilegiato con spoofing della
  firma limitato (patch a frameworks/base), `vetro-files` come servizio di
  init con dominio SELinux proprio, odex nelle partizioni, HAL di Cuttlefish
  senza host tolti o fermati. `tools/aosp`: sync, build sulla VM, fetch con
  hash e controllo di ART cortex-a53, disco GPT (`mkdisk.sh`), avvio sotto
  QEMU e Vetro con gli stessi slot, upload su R2
  (`aosp/android-15.0.0_r36-BP1A.250505.005.D1-9d91633/`, `manifest.json`),
  sorgenti GPL (kernel/common e virtual-device ai commit del manifest della
  build 12748506, progetti AOSP GPL/LGPL). **Home raggiunta sotto QEMU e
  sotto Vetro** (`vetro boot --jit`, 3 GiB, una CPU): `sys.boot_completed=1`
  a 476 s di guest in QEMU (~8 min reali), 801 s in Vetro (~1 h 15 min
  reali), adb via `--hostfwd` in tutti e due, launcher con microG Companion
  a schermo (screencap). Stesso insieme di servizi morti e stessi segnali nei
  due, nessun errore del kernel; `adb install` provato sotto QEMU.
- **Manca:** marchi nella UI di AOSP (barra "Google" di Launcher3, robot,
  "Phone is starting"); schermo sempre acceso di default (in
  `init.vetro.rc`, non ancora ricostruito); SELinux enforcing; verifica
  end-to-end dello spoofing con l'autotest di microG; barra di stato non
  ancora disegnata nello screencap di Vetro; ccache (la sandbox della build
  monta la home in sola lettura: andrebbe dentro `out/`); `HOST_CROSS_OS`
  linux_musl di Cuttlefish compila strumenti host inutili (ore di build).
- **Bloccato:** niente. La VM di build è ferma (disco conservato): serve
  riaccenderla per ricostruire con le modifiche già committate.
