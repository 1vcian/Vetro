# M4 — diario

## 2026-09-25 (JIT in modalità sistema, agente JIT)
- **Fatto:**
  - JIT della modalità sistema (ADR 0013): `vetro_jit::sys::SysJit` e
    `Machine::set_jit`. Blocchi per (`pc`, EL, TBI0/1, SPSel) validi per la
    pagina fisica del fetch (ritradotta a ogni ingresso dall'host con la MMU
    e la sua cache recente); `ld`/`st` dell'host attraverso `MmuBus` con i
    permessi di EL, SCTLR.A, big-endian, e fuori dalla RAM (MMIO) un fault
    che rifà l'interprete. La macchina chiama il JIT solo quando
    l'interprete non prenderebbe un interrupt e con i passi fino alla
    prossima scadenza del timer: stesse istruzioni, interrupt negli stessi
    punti. Invalidazione: bitmap delle pagine di codice in
    `vetro_machine::Ram` (CPU, DMA virtio, caricamento).
  - Velocità: dispatcher generato con cache dei salti ed `env.resolve`
    (nessun ritorno all'host fra blocchi noti), blocchi in una tabella di
    funzioni messa dal motore (`Engine::place`), moduli da 16 blocchi,
    TLB software per load/store allineati verso la RAM (nel browser la RAM
    del guest è nella memoria dei blocchi), uscite laterali nei salti
    condizionati, coda d'uscita comune, e nei blocchi anche esclusive,
    DC ZVA, CRC32 e MRS/MSR di TPIDR*, SP_EL0, TCR, DCZID, CurrentEL. In
    V8 il dispatcher si chiama da Rust come puntatore a funzione nella
    tabella esportata di vetro-wasm.
  - Motore JS: `impl vetro_jit::Engine for JsEngine` (ABI 2 di vetro-wasm),
    `web/node/jit-engine.mjs`, `boot.mjs --jit`, `tools/wasm-boot.sh --jit`
    (fallisce se il JIT in V8 supera l'interprete nativo), `vetro boot --jit`.
  - Tempi, avvio di `tests/boot` fino a `poweroff -f` (138767923
    istruzioni, M2 Pro con la macchina carica di altri build, dal
    caricamento allo spegnimento):

    | | Tempo |
    |---|---|
    | interprete nativo | 2,79–2,82 s |
    | interprete in V8 | 4,77 s |
    | **JIT in V8** | **1,52–1,53 s** (0,54× il nativo) |
    | JIT nativo (wasmtime) | 9,2 s (domina Cranelift: serve alla parità) |

    In V8 118,5 M istruzioni su 138,8 M girano nei blocchi (il resto: ~17 M
    di tempo saltato nelle WFI e ~3 M di codice freddo o non tradotto);
    12808 blocchi in 1192 moduli, 185 ms di compilazione.
  - Parità (tutte identiche all'interprete):
    - avvio del kernel col JIT su wasmtime (`VETRO_JIT=1`, test `vetro`
      che esegue entrambi) e su V8: stesse istruzioni, log uguale byte per
      byte;
    - sonda bare-metal di `tests/isa/system` col JIT (soglia 0): uscita
      uguale a QEMU, stesse istruzioni;
    - `vetro-jit-native/tests/sys_parity.rs`: 2000 programmi bare-metal
      casuali (400 in CI) con MMU, pagine non mappate/sola lettura/solo
      EL1/Device, EL0 ed EL1, eccezioni, esclusive, codice che si modifica,
      TLB software attiva: eccezioni, passi, CPU e RAM identici; un errore
      introdotto apposta nel percorso veloce o nel monitor viene trovato;
    - kselftest col JIT: stesso log e stesse 62136547024 istruzioni
      dell'interprete (95 test, esiti uguali a QEMU);
    - modalità utente dopo le modifiche al traduttore: `tools/ci.sh` con
      l'oracolo e tests/linux con `VETRO_JIT=1 VETRO_JIT_THRESHOLD=0` verdi.
  - CI (job `boot`): passo "Avvio sotto Vetro col JIT" e `tools/wasm-boot.sh
    --jit` con i tempi come annotazioni.
- **Manca:** la conferma della soglia nel job `boot` della CI (runner arm64:
  lì V8 era 2,1× il nativo contro 1,7× in locale); compilazione asincrona
  dei moduli nel browser; kselftest col JIT non ancora in CI (22 minuti in
  locale con QEMU e i due avvii).
- **Bloccato:** niente.

## 2026-09-25 (agente Web: la macchina in WebAssembly, avvio in Node)
- **Fatto:**
  - `vetro-wasm` esporta la macchina con un'API C senza dipendenze
    (`docs/specs/wasm.md`): alloc/free, macchina, `load_linux`, `run(budget)`
    con il motivo dell'arresto, console, istruzioni e tempo del guest.
  - `web/node/boot.mjs` (Node 22) esegue il copione di
    `tests/boot/tests/vetro.rs` sull'interprete compilato a wasm32. Risultato
    uguale al nativo: 98439742 istruzioni con `console=ttyAMA0`, log della
    console identico byte per byte a `vetro-boot.log`.
  - `tools/wasm-boot.sh` fa costruzione, ponte JIT, avvio nativo e avvio in
    Node, e confronta istruzioni e log. Gira nel job `boot` della CI con Node
    22 e stampa i due tempi come annotazioni.
  - Tempi su M2 Pro, Node v22.20.0, stesso avvio fino a `poweroff -f`:

    | Interprete | Tempo | Velocità |
    |---|---|---|
    | nativo (test `vetro`, release) | 1,94–2,02 s (2,12 s con la macchina carica) | ~50 MIPS |
    | wasm32 in V8 (dal caricamento allo spegnimento) | 3,33–3,38 s (3,9 s con la macchina carica) | ~29,5 MIPS, 1,7× il nativo |

    LTO e `codegen-units=1` sul .wasm non cambiano nulla (3,30–3,32 s). Il
    .wasm pesa 230 KB.
  - Ponte JIT predisposto: `web/node/jit-engine.mjs` (`JitEngine`,
    l'equivalente JS di `Engine`) e `vetro_wasm::jit::JsEngine`, collegati
    dagli import `vetro_jit.compile/run/drop` e dagli export
    `vetro_jit_ld/st`. `web/node/jit-selftest.mjs` prova il giro completo con
    un modulo di prova.
- **Manca:**
  - il JIT vero: traduttore (agente JIT), ciclo a blocchi nella `Machine`,
    `impl vetro_jit::Engine for JsEngine`;
  - la soglia di M4 (JIT in Node non più lento dell'interprete nativo): oggi
    l'interprete in V8 è a 1,7× il nativo;
  - un test Rust che esegua `boot.mjs`: per ora c'è lo script.
- **Bloccato:** niente. Da confermare con l'agente JIT come il blocco vede un
  fault di `ld` (qui: `FAULT` in `exit_detail`, offset 276).

## 2026-09-25 (JIT in modalità utente, agente JIT)
- **Fatto:**
  - `vetro-jit` (nessuna dipendenza, compila per wasm32): encoder WASM,
    `JitState` con gli offset della spec, traduttore, trait `Engine`/`Host`
    e `JitCpu`, che alterna blocchi tradotti e passi dell'interprete.
  - Copertura: aritmetica e logica (immediata, registro, estesa, con
    carry, coi flag), MOVZ/MOVN/MOVK, ADR/ADRP, bitfield, EXTR, CCMP/CCMN,
    CSEL e varianti, RBIT/REV/CLZ/CLS, divisioni e shift variabili,
    MADD/MSUB/SMADDL/UMADDL/SMULH/UMULH, MRS/MSR NZCV, tutti i salti,
    LDR/STR interi (immediato, pre/post, registro, letterale), LDP/STP/LDPSW,
    LDAR/STLR. Restano all'interprete: SIMD/FP, esclusive, CRC32, DC ZVA,
    registri di sistema diversi da NZCV, SVC/BRK (chiudono il blocco).
  - Precisione: un fault a metà blocco lascia lo stato di prima
    dell'istruzione, che l'interprete riprova (stessa eccezione, stesso
    indirizzo). Il budget di passi è esatto: il kernel emulato chiede al JIT
    al più i passi fino alla fine del quanto, al limite di istruzioni e
    alla prossima scadenza di un alarm (tempo virtuale), e uno solo se c'è
    un segnale da consegnare. Stesso orologio: stesso numero di istruzioni
    con e senza JIT (202475874 per `sha256sum` di 4 MB).
  - Invalidazione (progetto in `crates/vetro-jit/src/driver.rs`):
    `UserMemory` ha un `space_id` (nuovo a ogni copia, cioè a ogni fork) e
    sorveglia le pagine da cui il JIT ha tradotto; scritture del guest e
    del kernel emulato (`write`, `poke`), mmap, munmap, mprotect, mremap e
    la crescita dello stack le segnano sporche. Il driver le raccoglie
    prima di ogni blocco e dopo ogni passo dell'interprete; uno store di un
    blocco su una pagina sorvegliata esce con STOP dopo l'istruzione. Il
    codice delle MAP_SHARED non si traduce. I moduli compilati si riusano
    fra spazi per (pc, parole).
  - ABI come nella spec aggiornata su main: dopo ogni `ld` il blocco
    controlla `exit_detail` (1 = FAULT, risposta alla domanda dell'agente
    Web), `st` lascia 2 per STOP; `pc` e `steps` sono salvati prima di ogni
    accesso.
  - `vetro-jit-native`: motore su wasmtime 49. wasmtime tiene al più 10000
    istanze per store e non le libera: quando `compile` fallisce il driver
    scarta tutto e chiama `Engine::reset` (store nuovo).
  - `vetro run --jit [--jit-threshold=N] [--stats]`,
    `Config { jit, jit_threshold }`; gli harness di tests/linux leggono
    `VETRO_JIT=1` e `VETRO_JIT_THRESHOLD`.
  - Parità (tutti con `VETRO_REQUIRE_ORACLE=1` e l'oracolo Docker):
    - `vetro-jit-native/tests/parity.rs`: 3000 programmi casuali in
      processo (20000 a mano) con cicli, fault, codice che si modifica da
      sé, SVC e budget spezzati: eccezioni, passi, registri e memoria
      identici all'interprete; il test fallisce se una sola differenza (un
      errore introdotto apposta nel flag C viene trovato);
    - `tests/diff` (random, simd, fp, crypto: 1300 programmi) ora gira anche
      col JIT (soglia 0): 0 differenze tra interprete, JIT e QEMU;
    - `tests/isa`: ogni caso gira anche col JIT e deve dare lo stesso esito
      dell'interprete: tutti verdi;
    - tests/linux con `VETRO_JIT=1` (soglia 16 e soglia 0): BusyBox (30),
      regress, RISU verdi; LTP rapido verde.
    - LTP completo (`VETRO_LTP_FULL=1`) su macOS con l'oracolo Docker, col
      JIT e con l'interprete: 339 test; 322 identici a QEMU col JIT, 326
      con l'interprete. Le differenze sono tutte dal lato di QEMU nel
      container (umask01, fcntl12/15, timer, uscite 125 dell'oracolo: gli
      artefatti del bind mount già noti da M2) e cambiano da un giro
      all'altro; l'esito di Vetro è lo stesso col JIT e con l'interprete in
      ogni test diverso, e i sei test con esito di QEMU instabile
      (fcntl34-36, wait403) rieseguiti col JIT danno lo stesso esito
      dell'interprete. Il giudice resta il job linux della CI (host arm64).
  - Velocità (release, stesso binario, stesso numero di istruzioni con e
    senza JIT, soglia 16):

    | Programma (BusyBox) | Istruzioni | Interprete | JIT | |
    |---|---|---|---|---|
    | `sha256sum` di 4 MB | 202,5 M | 25,2 MIPS | 158,0 MIPS | 6,3× |
    | `gzip -c` di 4 MB | 623,8 M | 17,7 MIPS | 54,8 MIPS | 3,1× |
    | `dc -e "2 30000 ^ p"` | 160,4 M | 18,1 MIPS | 51,2 MIPS | 2,8× |
    | `awk` (ciclo di 2 M) | 10092 M | 16,7 MIPS | 38,1 MIPS | 2,3× |

    Col JIT il 99,5% dei passi gira nei blocchi; gzip e dc fanno 5-7
    istruzioni per blocco, e ogni corsa di blocco costa circa 90 ns
    (chiamata wasmtime, ricerca del blocco, stato): il prossimo guadagno è
    concatenare i blocchi dentro il modulo. Nel profilo di sha256sum circa
    il 30% del tempo è in `UserMemory::read/write` (ricerca della regione
    nel BTreeMap), il 20% nell'ingresso e uscita di wasmtime.
- **Manca:**
  - modalità sistema: blocchi per pagina fisica + EL + regime, controllo
    della traduzione VA→PA all'ingresso, `ld`/`st` attraverso la MMU con i
    permessi del livello (e un flag "non privilegiato" nell'ABI per
    LDTR/STTR, oggi assente: in modalità sistema vanno lasciate
    all'interprete), controllo dell'allineamento di SP (SCTLR.SA/SA0),
    TBI sui salti a registro, budget fino al prossimo evento della
    piattaforma e IRQ controllati tra un blocco e l'altro, invalidazione
    per le scritture fisiche (DMA virtio, alias);
  - motore JavaScript per `vetro-wasm` (lo collega il coordinatore);
  - job della CI con `VETRO_JIT=1` su tests/linux (proposto, non aggiunto);
  - velocità: concatenamento dei blocchi, accesso alla memoria senza la
    ricerca della regione a ogni `ld`/`st`, più blocchi per modulo.
- **Bloccato:** niente.

## 2026-09-26 (chiusura)
- **Criterio di uscita:** verde in CI (run 36174581958).
  - Test M1/M2 col JIT nel job linux: LTP completo, BusyBox, RISU e regress
    con `VETRO_JIT=1`, soglia 0.
  - Parità interprete-JIT: programmi casuali in modalità utente e di
    sistema, sonda di sistema, avvio del kernel con lo stesso log e le stesse
    istruzioni.
  - Avvio col JIT in V8 di 3,85 s contro 4,36 s dell'interprete nativo, nello
    stesso job (138767923 istruzioni).
- **Manca:** il margine sul runner è stretto (circa 12%, in locale 0,54×).
  Da fare:
  - compilazione asincrona dei moduli nel browser;
  - blocchi più lunghi e allocazione dei registri;
  - kselftest col JIT in CI (circa 22 minuti).
- **Bloccato:** niente.

## 2026-09-26 (JIT: regioni e prestazioni, agente JIT, ADR 0024)
- **Profilo di partenza** (avvio di `tests/boot` col JIT in V8, M2 Pro con
  la macchina carica di altri agenti; `sample` di macOS, profilo di V8,
  contatori):
  - delle 256,8 M istruzioni dell'avvio (initramfs di allora) 128,6 M sono
    tempo saltato nelle WFI, 125,5 M girano nei blocchi, 2,7 M
    nell'interprete (2,44 M a freddo, 0,25 M dopo un'uscita);
  - thread principale: il 25% è compilazione (validazione dei moduli e
    compilazione pigra di Liftoff al primo uso, proporzionale ai byte:
    14,7 MB di WASM, ~1,1 KB per blocco, 150-370 byte per istruzione di
    memoria); le prime regioni, più grandi, la portavano al 37%. TurboFan sui
    thread di fondo: `--no-liftoff` 6,7 s, `--liftoff-only` 2,5 s contro
    1,85 s di default;
  - il tempo "del dispatcher" nel profilo di V8 è la compilazione pigra
    attribuita al chiamante: il dispatch vero costa poco (~17 istruzioni per
    chiamata di regione a regime, con i contatori);
  - uscite verso l'interprete (stessa istrumentazione, avvio più un ciclo di
    shell): 590 mila su 4 classi SIMD in EL0 (`memcpy`/`memset` di musl:
    LDR/STR/LDP/STP Q, DUP/INS/UMOV, MOVI), 82 mila su DAIF, 33 mila su
    ELR/SPSR/ESR/FAR, 64 mila accessi MMIO; flag NZCV e uscite con due
    chiamate per regione; ogni accesso alla memoria una chiamata.
- **Fatto** (ADR 0024, `docs/specs/jit.md` aggiornata, vetro-wasm ABI 10):
  - regioni per pagina con cicli dentro la funzione (`loop` + `br_table`),
    ingressi multipli (niente codice duplicato), limite di passi per blocco
    base (istruzioni esatte, interrupt e replay invariati);
  - codice compatto: runtime condiviso `rt.*` (percorsi lenti, coppie, Q,
    TLB dei non allineati, `nzcv`, `finish`, `vsync`), percorso veloce della
    TLB in linea, uscite corte, forme dirette per ADD/SUB/MOV/UBFM/SBFM,
    controllo di SP una volta per blocco base;
  - flag pigri (dentro la regione e fra le regioni, in `JitState`);
  - SIMD nelle regioni (LDR/STR B..Q, LDP/STP S/D/Q, DUP, INS, UMOV/SMOV,
    MOVI/MVNI/ORR/BIC) con i registri V copiati solo quando servono
    (`env.vsync`), Q con le regole dell'accesso intero, TLB dei non
    allineati; DAIF, ELR/SPSR/ESR/FAR a EL1 con `YIELD` dopo uno
    smascheramento;
  - host: il codice freddo non si traduce con la MMU; `ctx` con tutti i
    parametri della regione (prima una voce poteva valere con SPSel o FP
    diversi); soglia 64 (misurate 16/32/64/128 nell'avvio: 1,88/1,72/1,62/
    1,65 s);
  - `vetro-cpu` esporta `simd::{CopyOp, MovImmOp}` (solo i nomi dei tipi).
- **Misure prima/dopo** (stesso binario di riferimento costruito da main
  854303a, stesso initramfs; migliore di 2-3 giri, macchina carica):

  | Avvio di `tests/boot` (284,8 M istruzioni) | prima | dopo |
  |---|---|---|
  | JIT in V8 (`tools/wasm-boot.sh --jit`) | 1,91 s | **1,55-1,63 s** |
  | moduli compilati in V8 | 14,9 MB, 13630 blocchi | 10,1 MB, 6537 regioni |
  | CPU di tutti i thread (V8) | 22,7 G istruzioni | 26,2 G |
  | JIT nativo (wasmtime, `vetro boot --jit`) | 10,25 s | 9,78 s |
  | interprete nativo | 4,42 s | 4,42 s |

  A regime nel guest, in V8 (kernel M3 + BusyBox, secondo giro dopo il
  riscaldamento, `node guest.mjs` in scratch):

  | Comando nel guest | prima | dopo |
  |---|---|---|
  | `sha256sum /bin/busybox` (61 M istr.) | 577 MIPS | **793 MIPS** |
  | `gzip -c /bin/busybox` (323 M) | 361 MIPS | **522 MIPS** |
  | ciclo di shell da 20000 giri (309 M) | 109 MIPS | **217 MIPS** |

  Programmi utente di M4 (`vetro run --jit`, wasmtime; interprete nativo
  per confronto):

  | Programma (BusyBox) | Istruzioni | interprete | JIT prima | JIT dopo |
  |---|---|---|---|---|
  | `sha256sum` di 4 MB | 212,3 M | 23,2 MIPS | 135 MIPS | 168,5 MIPS |
  | `gzip -c` di 4 MB | 654,1 M | 16,2 MIPS | 50,2 MIPS | 59,3 MIPS |
  | `dc -e "2 30000 ^ p"` | 160,4 M | 16,6 MIPS | 45,1 MIPS | 52,7 MIPS |
  | `awk` (ciclo di 200000) | 984,3 M | 15,4 MIPS | 36,3 MIPS | 41,8 MIPS |

  Dimensione del codice (test `codice_compatto`, istruzioni tipiche del
  kernel a EL1): 66 byte per istruzione, contro ~130 prima.
- **Parità:** `parity.rs` e `sys_parity.rs` (ora con SIMD, DAIF, ELR/SPSR,
  UMA casuale; 20000 programmi utente e 3000 di sistema a mano) verdi; ogni
  punto nuovo ha un errore introdotto apposta che il test trova (flag pigri
  dell'host, MOVI, metà di un Q, Q allineato a 8 su Device, TLB dei non
  allineati riempita da accessi allineati, codice interno di STOP uguale a
  YIELD: trovato davvero durante il lavoro). Test mirati nuovi: Q a cavallo
  di pagina, accesso non allineato che sconfina dalla TLB dei non allineati,
  YIELD dopo DAIFClr, regioni con cicli. `cargo test --workspace` con
  l'oracolo (`VETRO_REQUIRE_ORACLE=1`; tests/diff col JIT verdi, i semi
  214, 215, 220 SIMD e 210 FP scaduti una volta per QEMU in Docker oltre 30 s
  sotto carico, rieseguiti uno alla volta: verdi), `VETRO_JIT=1` tests/linux
  e avvio del kernel, replay e snapshot col JIT, build wasm,
  `tools/wasm-boot.sh --jit`, `tools/web-test.sh`, clippy e fmt.
- **Manca:** concatenamento in modalità utente (`JitCpu` chiama il motore
  per ogni regione: 27 M chiamate per `gzip`); un secondo livello per le
  regioni calde (più grandi, 256 istruzioni danno +10% a regime ma +40% di
  CPU di compilazione); compilazione asincrona (sposterebbe solo la
  validazione, vedi ADR 0024); altre istruzioni SIMD/FP (ART e bionic ne
  useranno molte di più); CNTVCT nelle regioni.
- **Bloccato:** niente.

## 2026-09-26 (JIT: FP/SIMD nelle regioni, orologio, modalità utente; agente JIT, ADR 0026)
- **Misure di partenza** (contatori per classe nuovi, `VETRO_JIT_PROFILE=1`):
  - `tests/linux/c/fpsimd.c` (nuovo: conversioni, matrici float/double,
    memcpy/strlen/memchr NEON, TBL, EXT, ADDV/UMAXV, FPCR/FPSR; confrontato
    con QEMU) con `vetro run --jit`: 31 dei 41 M di istruzioni
    all'interprete (76%), soprattutto FMOV verso i registri generali, LD1R,
    FMLA vettoriale e per elemento, FADD/FMUL/FDIV/FMADD, FCVT; JIT 21 MIPS
    contro 20 dell'interprete;
  - avvio di `tests/boot`: all'interprete quasi solo codice freddo; fra le
    istruzioni non tradotte MRS CNTVCT (17 mila), ERET, MMIO;
  - `awk` e `dc` di BusyBox: LDXR/STXR (in modalità utente non tradotte) e
    FMOV/FADD/SCVTF.
- **Fatto** (ADR 0026, `docs/specs/jit.md`, vetro-wasm ABI 11):
  - ogni istruzione SIMD/FP senza memoria nelle regioni: SIMD intero esatto
    in linea con WASM SIMD (anche le saturanti con QC), FMOV/FABS/FNEG/FCSEL
    in linea, percorsi veloci `rt.fp<k>` per l'aritmetica FP (scrivono solo
    con FPCR = 0, senza NaN, minuscoli o trabocchi e con IXC a 1 o risultato
    esatto; FMA in singola con l'arrotondamento a dispari, in doppia con
    l'emulazione di Boldo e Melquiond), il resto con `env.simd`
    (l'interprete sui registri di `JitState`);
  - LD1/ST1 di 1-4 registri, LD1R, LD1/ST1 di una corsia, LD2..LD4/ST2..ST4;
  - MRS CNTPCT/CNTVCT nelle regioni col numero di istruzioni della macchina
    (`SysJitDyn::set_time`), a EL0 secondo CNTKCTL_EL1;
  - modalità utente: regioni concatenate col dispatcher (contesto per
    spazio), esclusive tradotte;
  - secondo livello provato e scartato (misure nell'ADR 0026).
- **Misure prima/dopo** (M2 Pro condiviso, migliore di 2-3 giri; "prima" =
  main 10c1815):

  | `vetro run --jit` (wasmtime) | Istruzioni | interprete | JIT prima | JIT dopo |
  |---|---|---|---|---|
  | `fpsimd 20` | 40,7 M | 20,0 MIPS | 21,0 MIPS | **52,5 MIPS** |
  | `sha256sum` di 4 MB | 212,3 M | 22,8 MIPS | 167,9 MIPS | 167,6 MIPS |
  | `gzip -c` di 4 MB | 1023 M | 16,3 MIPS | 71,7 MIPS | **76,0 MIPS** |
  | `dc -e "2 30000 ^ p"` | 160,4 M | 16,4 MIPS | 51,8 MIPS | 52,9 MIPS |
  | `awk` (ciclo di 200000) | 1008 M | 15,6 MIPS | 43,4 MIPS | **52,4 MIPS** |

  Chiamate al motore per `gzip`: da 27 M a 59 mila (concatenamento); il
  limite ora sono i load/store, ognuno una chiamata all'host.

  | JIT in V8 | prima | dopo |
  |---|---|---|
  | avvio di `tests/boot` (`tools/wasm-boot.sh --jit`) | 1,67-1,69 s | 1,61-1,68 s |
  | moduli compilati nell'avvio | 10,07 MB | 10,11 MB |
  | `awk` FP nel guest (3022 M istr., a regime) | 177 MIPS | **270 MIPS** |
  | `sha256sum /bin/busybox` nel guest | 779 MIPS | 771 MIPS |
  | `gzip -c /bin/busybox` nel guest | 516 MIPS | 498-526 MIPS |
  | ciclo di shell (20000 giri) | 224 MIPS | 220 MIPS |

- **Parità:** `parity.rs` (registri V, FPCR, FPSR casuali; programmi per tre
  quarti SIMD/FP con load/store V; esclusive in modalità utente;
  concatenamento dopo un'invalidazione), `fp_limits.rs` (470 mila casi
  limite, FMA casuali, doppio arrotondamento, minuscolo arrotondato al
  normale, percorsi veloci usati davvero), `sys_parity.rs` (CNTPCT/CNTVCT,
  CNTKCTL, secondo livello quando c'era), `tests/diff` con i semi nuovi
  `fpfast-` contro QEMU. Ogni fast path ha un errore introdotto apposta
  che un test trova (elenco nell'ADR 0026); il test che conta le chiamate a
  `env.simd` ha trovato un errore vero (IXC mai considerato a 1).
- **Verifica:** clippy -D warnings e fmt; `cargo test --workspace` con
  l'oracolo (`VETRO_REQUIRE_ORACLE=1`; i 5 test di `tests/diff/random.rs`,
  scaduti per QEMU in Docker oltre 30 s col Mac carico, rieseguiti uno alla
  volta: verdi, compresi i semi `fpfast-`); `VETRO_JIT=1` tests/linux
  (anche `fpsimd`) e avvio del kernel, replay e snapshot col JIT; build
  wasm; `tools/wasm-boot.sh --jit` (JIT in V8 1,57 s contro 4,38 s
  dell'interprete nativo, stesse istruzioni e stesso log);
  `VETRO_REQUIRE_BROWSER=1 tools/web-test.sh`.
- **Manca:** TLB software in modalità utente (la memoria del guest nella
  memoria del motore: ogni load/store è ancora una chiamata all'host); FMA
  in doppia fuori dagli intervalli dell'emulazione e le altre operazioni
  FP con FPCR diverso da zero passano da `env.simd`; LDR letterale dei
  registri V e strutture singole interlacciate all'interprete; regioni più
  grandi da rivalutare con un carico Android vero (ADR 0026).
- **Bloccato:** niente.
