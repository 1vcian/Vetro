# M7 — diario

## 2026-09-25 (analisi di rete senza Android, agente Analisi)
- **Fatto:** punto di cattura su virtio-net (`Machine::net_tap`, tempo virtuale, fuori dagli snapshot) e `vetro-analysis::net` senza dipendenze (anche wasm32): pcapng, flussi TCP/UDP ricostruiti, DNS, HTTP/1.1 con chunked e gzip/deflate (DEFLATE nostro), corpi JSON/form/multipart/protobuf senza schema/gRPC, ispettore con timing, HAR 1.2; `vetro boot --pcap --har --net-requests`; test con il kernel guest (7 richieste wget/nc di BusyBox, pcapng e HAR identici in due esecuzioni) verificati con capinfos/tcpdump/tshark, har-validator (schema 1.2) e haralyzer in Docker (`tools/analysis/check.sh`). ADR 0016, `docs/specs/analysis.md`.
- **Manca:** hook TLS su BoringSSL e Conscrypt (serve Android), timeline input→effetti, esportazioni e ispettore in vetro-wasm e nell'app web, Brotli/zstd, HTTP/2, IPv6; uscita di M7 sulle 10 app.
- **Bloccato:** l'uscita di M7 aspetta Android (M5/M6); `wget --post-file` di BusyBox tronca i corpi binari al primo byte zero, per quelli il test usa `nc`.

## 2026-09-26 (ispettore di rete e timeline nell'app web, agente Web)
- **Fatto:** ABI 8 di vetro-wasm (ADR 0023): cattura nella macchina a ogni quanto, lista e dettaglio in JSON da `vetro_analysis::net::view`, HAR e pcapng; `vetro_analysis::timeline` (ingressi con istruzione, effetti http/dns/tls/file/console nel tempo del guest, attribuzione "ultimo ingresso di comando entro 3 s" dichiarata come euristica, righe della console ricostruite, JSON) con test unitari; nell'app i pannelli Rete (lista, filtri, dettaglio con corpi JSON/form/multipart/protobuf/esadecimale, esportazioni HAR e pcapng scaricabili) e Timeline (ingressi con i loro effetti, asse del tempo, "vai qui"); comandi del gestore dei file come ingressi e eventi di inotify come effetti. Test: `cargo test -p vetro-analysis`/`-p vetro-wasm`, `tests/web/inspector.mjs` (wget al sinkhole, corpi decodificati, rete attribuita al comando, due esecuzioni uguali), `tests/web/browser-analysis.mjs` in Chrome (richiesta nell'ispettore col JSON decodificato e legata al comando digitato, scrittura di un file legata al suo comando, download veri), `tests/web/pages.mjs` (l'ispettore del sito vede la rete).
- **Manca:** hook TLS su BoringSSL e Conscrypt e l'uscita sulle 10 app (serve Android); attribuzione causale per processo (con i tracer di M8–M9 al posto dell'euristica temporale); effetti sui file nella timeline di un replay (il client del gestore è chiuso durante il replay); Brotli/zstd, HTTP/2, IPv6.
- **Bloccato:** niente (l'uscita di M7 aspetta Android, M5/M6).

## 2026-09-26 — introspezione dall'esterno, base di M7/M8/M9 (agente Analisi, ADR 0027)
- **Fatto:** lettura del sistema operativo guest dalla sola memoria fisica,
  senza toccarlo. `vetro-analysis::introspect`: parser BTF nostro (offset
  delle strutture, anche dentro unioni anonime), kallsyms dall'`Image`
  (tabella dei token dalle cifre, RELA/RELR, base relativa) o System.map,
  walker delle tabelle delle pagine (4 KiB, sola lettura), lettura del
  kernel Linux (processi/thread come `ps`, VMA come `/proc/<pid>/maps`, file
  aperti, cmdline, page cache con `vmemmap` calibrato), simboli ELF utente
  (file e memoria, DT_HASH/GNU_HASH), decoder syscall (con socket, connect,
  ioctl) e binder grezzo (BINDER_WRITE_READ → transazioni col Parcel e il
  descrittore d'interfaccia). Agganci in `vetro-machine` (`hooks.rs`,
  `introspect.rs`): tracciamento SVC di EL0 e ritorno (chiave SP_EL1),
  punti d'arresto invisibili su indirizzi utente (nessun BRK), col JIT
  `SysJit::set_stops` che tiene quegli indirizzi fuori dalle regioni (aggancio
  minimo, con prova di parità). BTF staccato del kernel di prova in
  `tools/guest-kernel/build.sh` (pahole, l'`Image` non cambia). Test
  `tests/boot/tests/introspect.rs`: processi = `ps`, maps = `/proc`, fd,
  simboli dalla page cache = file host, syscall di `cat` (utente 501) =
  `qemu-aarch64 -strace`, punti d'arresto = syscall corrispondenti,
  determinismo con/senza agganci e replay identico col JIT (+1,6% interprete,
  ~0% col JIT senza i confronti); `profili_dei_kernel` legge anche il GKI da
  `boot.img` (binder_proc/binder_transaction nel BTF). ADR 0027,
  `docs/specs/introspection.md`.
- **Manca:** hook TLS su BoringSSL/Conscrypt (sopra i punti d'arresto);
  prova lunga su Android (`VETRO_ANDROID=1`): processi con system_server/
  zygote e una transazione binder catturata (serve un avvio ~1 h). Filtro
  binder per /dev/binder dal percorso del fd, non solo dal codice ioctl.
- **Bloccato:** niente.
