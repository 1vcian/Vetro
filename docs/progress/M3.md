# M3 — diario

## 2026-09-24 (preparazione, agente Piattaforma)
- **Fatto:** `vetro-platform` con bus MMIO, PL011, PL031, GICv3 (distributore, redistributore, interfaccia CPU come funzioni Rust, solo gruppo 1), timer generico (CNTP/CNTV, PPI 27/30), slot virtio-mmio vuoti, `Virt` montata e generatore DTB v17 con il device tree della piattaforma; spec `docs/specs/platform.md`; 53 test unitari, clippy e build wasm32 verdi; DTB decompilato senza errori da `dtc`.
- **Manca:** collegamento alla CPU (EL1, MRS/MSR su ICC_* e CNT*, eccezioni IRQ, PSCI via HVC), virtio-blk e virtio-net veri, caricatore del kernel, confronto dell'avvio con `qemu-system-aarch64`.
- **Bloccato:** niente; il cablaggio dipende da EL1 e MMU in `vetro-cpu`/`vetro-mmu` (agente CPU).

## 2026-09-24 (preparazione MMU)
- **Fatto:** `vetro-mmu` con traduzione stage 1 EL1&0 a 4 KiB (TTBR0/TTBR1, TxSZ 16..39, TBI, blocchi 2 MiB/1 GiB, AP/APTable, UXN/PXN, WXN, AF, nG/ASID, MAIR), fault con DFSC/IFSC, ESR, FAR e PAR, TLB software con tutte le TLBI EL1, adattatore `VirtMemory` per `vetro_cpu::Memory`; spec `docs/specs/mmu.md`; 33 test unitari verdi, clippy e build wasm32 verdi.
- **Manca:** collegamento alla CPU (EL1, MSR/MRS dei registri, TLBI/AT nel decoder, consegna degli abort con ESR/FAR), flush del TLB alla scrittura di SCTLR/TCR, confronto con `qemu-system-aarch64` durante l'avvio del kernel; granulo 64 KiB e descrittori big-endian restano `Unimplemented`.
- **Bloccato:** niente.

## 2026-09-24 (preparazione virtio, agente Piattaforma)
- **Fatto:** in `vetro-platform` trasporto virtio-mmio v2, virtqueue split (catene, INDIRECT, EVENT_IDX), virtio-blk (backend in memoria e copy-on-write, richieste sospese con `NotReady`), virtio-net (MAC, STATUS, MRG_RXBUF), virtio-console a una porta; montaggio negli slot di `Virt` con linee SPI al GIC; trait `GuestRam`, `BlockBackend`, `NetBackend`, `ConsoleBackend`; driver di prova stile Linux; 102 test verdi, clippy e build wasm32 verdi.
- **Manca:** chiamata di `service_virtio` dal motore (dopo gli accessi MMIO agli slot e periodica) con una `GuestRam` sopra la memoria della CPU; backend veri (disco via HTTP Range/OPFS, rete, adb); confronto con un kernel vero su `qemu-system-aarch64` (probe di virtio_blk/virtio_net/virtio_console).
- **Bloccato:** niente; la prova col kernel aspetta il collegamento CPU-piattaforma (EL1, IRQ).

## 2026-09-24 (Rete, preparazione)
- **Fatto:** `vetro-net` da stub a stack di rete lato host (ADR 0007, spec
  `docs/specs/net.md`): gateway virtuale come la rete user di QEMU (ARP,
  DHCP server 10.0.2.15/.2/.3, ICMP echo, port unreachable), TCP e UDP
  terminati lato host per ogni connessione del guest, trait `Upstream` con
  `Sinkhole` (registra connessioni e byte, risposte per porta, DNS verso
  indirizzi finti 198.18.x.y in ordine di domanda) e `RelayUpstream` +
  `MemoryRelay` (solo interfaccia e prova in memoria), registro eventi con
  tempo virtuale. Deterministico (tempo come parametro, ISN dal seme,
  `BTreeMap`), nessuna dipendenza a runtime; `smoltcp::wire` solo nei test
  come parser indipendente. 34 test verdi (`cargo test -p vetro-net`),
  clippy, fmt e build wasm32 verdi. Il trasferimento contemporaneo nei due
  versi è stato provato a mano anche su 2000 semi con il 30% e il 45% di
  frame persi (ritrasmissioni illimitate): tutto consegnato e chiuso pulito;
  in CI girano 12 semi al 20%. Trovati e corretti lungo la strada: sonda a
  finestra zero che metteva un byte in volo (ora segmento vuoto con
  sequenza già riscontrata, come Linux) e RTO che restava raddoppiato dopo
  una raffica di perdite (ora un ACK di dati nuovi lo riporta al valore
  calcolato, come BSD/Linux), entrambi con test.
- **Manca:** collegamento a virtio-net (agente Piattaforma: `receive` per
  ogni frame del guest, `pop_frame` verso il guest, `poll` a
  `next_deadline`; non offrire `VIRTIO_NET_F_CSUM`); prova con un kernel
  Linux vero (udhcpc, wget, nslookup); IPv6 e frammenti IPv4 non gestiti
  (documentato); relay WebSocket vero, codifica dei messaggi e controllo di
  flusso host↔relay (M7); stato serializzabile per gli snapshot (M6).
- **Bloccato:** niente.
