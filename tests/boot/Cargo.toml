[package]
name = "vetro-boot-tests"
description = "Avvio del kernel guest di M3 (Image + initramfs) sotto qemu-system-aarch64, e da M3 sotto Vetro."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dev-dependencies]
vetro-machine = { path = "../../crates/vetro-machine" }
vetro-platform = { path = "../../crates/vetro-platform" }
# Il JIT della modalità sistema su wasmtime (VETRO_JIT=1, ADR 0013).
vetro-jit-native = { path = "../../crates/vetro-jit-native" }

[lints]
workspace = true
