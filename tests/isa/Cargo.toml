[package]
name = "vetro-isa-tests"
description = "Test per istruzione della CPU, con valori attesi verificati anche contro QEMU."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
vetro-diff = { path = "../diff" }

[dev-dependencies]
vetro-cpu = { path = "../../crates/vetro-cpu" }
vetro-mmu = { path = "../../crates/vetro-mmu" }
# La sonda di sistema anche col JIT della modalità sistema (ADR 0013).
vetro-jit = { path = "../../crates/vetro-jit" }
vetro-jit-native = { path = "../../crates/vetro-jit-native" }
