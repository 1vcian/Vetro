[package]
name = "vetro-linux-tests"
description = "Test differenziali di binari Linux arm64 (BusyBox, programmi C, LTP) contro QEMU."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
vetro-cli = { path = "../../crates/vetro-cli" }
vetro-diff = { path = "../diff" }
