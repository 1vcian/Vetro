[package]
name = "vetro-machine"
description = "La macchina virt completa: CPU in modalità sistema, MMU, piattaforma e RAM, con tempo deterministico."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
vetro-cpu = { path = "../vetro-cpu" }
vetro-mmu = { path = "../vetro-mmu" }
vetro-platform = { path = "../vetro-platform" }
vetro-jit = { path = "../vetro-jit" }
vetro-net = { path = "../vetro-net" }
vetro-snapshot = { path = "../vetro-snapshot" }
# Introspezione (ADR 0027): senza dipendenze, compila anche in wasm.
vetro-analysis = { path = "../vetro-analysis" }

[lints]
workspace = true
