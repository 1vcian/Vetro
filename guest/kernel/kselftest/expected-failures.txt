# kselftest che falliscono anche sotto QEMU per l'ambiente del guest, non per
# la CPU o la piattaforma. Il test tests/boot/tests/kselftest.rs pretende che
# ogni altro test della selezione sia ok o SKIP, e che l'esito di tutti
# coincida con QEMU. Formato: gruppo:test  # motivo
arm64:fp-ptrace                  # PTRACE_GETREGSET di FPSIMD: ENOSYS con questa configurazione del kernel
exec:execveat                    # 13 sottocasi su 54 eseguono `script` (#!/bin/bash): bash non c'è, ENOENT
exec:non-regular                 # 1 sottocaso su 6 (S_IFLNK.exec_errno) fallisce anche sotto QEMU
exec:load_address.0x1000         # compilato statico: manca l'header INTERP che il test cerca
exec:load_address.0x200000       # come sopra
exec:load_address.0x1000000      # come sopra
exec:load_address.static.0x1000  # SIGSEGV anche sotto QEMU: il test usa -static-pie e il nostro CC aggiunge -static
exec:load_address.static.0x200000 # come sopra
exec:load_address.static.0x1000000 # come sopra
exec:check-exec                  # 4 sottocasi su 32 aprono un dispositivo a blocchi: ENXIO, il kernel guest non ne ha
exec:binfmt_script.py            # serve python3, assente nell'initramfs
exec:check-exec-tests.sh         # serve bash, assente nell'initramfs
mincore:mincore_selftest         # O_TMPFILE sulla radice (ramfs) non supportato
timers:mqueue-lat                # CONFIG_POSIX_MQUEUE spento nel kernel guest
timers:rtcpie                    # senza CONFIG_HIGH_RES_TIMERS (HZ=100) il periodo a 16 Hz è 70 ms invece di 62,5: stesso valore sotto QEMU e Vetro
