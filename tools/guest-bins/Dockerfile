# Toolchain per i binari guest di test: Alpine arm64, musl, link statico.
FROM alpine:3.22
RUN apk add --no-cache build-base linux-headers busybox-static perl git \
    autoconf automake m4 libtool pkgconf bash
